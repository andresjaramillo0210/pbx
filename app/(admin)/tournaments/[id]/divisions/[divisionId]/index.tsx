// Admin division page — the same view spectators see, but every match is
// tappable so the admin can enter scores. All setup actions (teams, courts,
// format, generate matches, etc.) live on dedicated sub-routes
// (/edit, /generate, /teams/...).
export { default } from './_views/court-board';
