// Public-facing Court Board view of a division. Thin re-export of the
// implementation in `_views/court-board.tsx` so the view module can be
// shared/imported without going through the route layer.
export { default } from './_views/court-board';
